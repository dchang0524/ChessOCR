{
  "name": "chessocr-browser",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/browser_inference.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
